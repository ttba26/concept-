/** @type {import('next').NextConfig} */
const nextConfig = {
  transpilePackages: ["@your-scope/ui", "@your-scope/tokens"],
  reactStrictMode: true,
  typescript: { ignoreBuildErrors: true },
  eslint: { ignoreDuringBuilds: true },
};
export default nextConfig;
/** @type {import('next').NextConfig} */
const nextConfig = {
  transpilePackages: ["@your-scope/ui", "@your-scope/tokens"],
  reactStrictMode: true,
  typescript: { ignoreBuildErrors: true },
  eslint: { ignoreDuringBuilds: true },
};

export default nextConfig;

